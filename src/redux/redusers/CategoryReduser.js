const initialState = {
    categoryName: [
        'Комбо-сеты',
        'Салаты',
        'Десерты',
        'Напитки',
        'Соусы'
      ],
}

const CategoryReduser = (state = initialState, action) => {
    
    return state;
}
export default CategoryReduser;